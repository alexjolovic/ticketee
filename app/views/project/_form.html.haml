= simple_form_for @project, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :description
  = f.button :submit, :class => 'btn'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), project_path, :class => 'btn'
